# Upstream Notice

`SKILL.md`、`scenes/`、`templates/` 和 `tokens/` 来自本机安装的 TRAE CN 全局内置 `dynamic-ui` Skill，并按 2026-09-13 的本地版本原样保存。

上游目录没有随附独立的 `LICENSE`、`NOTICE` 或 `COPYING` 文件。本目录中的上游内容仍归 TRAE 或其相应权利人所有；本仓库根目录的 MIT License 不应被理解为对这些上游文件重新授权。

本次封装新增的文件只有 `README.md` 和本说明。若后续获得明确的上游许可文件，应以该许可为准并同步更新本说明。
