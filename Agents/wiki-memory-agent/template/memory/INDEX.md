# 记忆索引

> 每个新用户回合都先读取本文件。这里是路由地图，不是记忆正文；禁止默认读取 `memory/` 下的全部文件。

## 路由方法

- 使用每个条目的类型、范围、触发词、状态和摘要匹配当前任务。
- 只打开当前任务必需的少量链接页面；只有证据仍不足时才继续沿链接展开。
- 记忆过时或冲突时，以当前用户要求和实际工作区为准。

## 条目格式

`- [标题](relative/path.md) — 类型：type；范围：受影响区域；触发：关键词、路径；摘要：一句话；状态：active；更新：YYYY-MM-DD`

## 活动条目

目前还没有持久项目记忆。
